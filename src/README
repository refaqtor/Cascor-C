Toolkit is an implementation of a library, toolkit.h, that contains helper functions that serve as tools for the network.
Parse is an implementation of a parser for the neural network.
Cascade is the directory in which the cascade-correlation network is implemented
